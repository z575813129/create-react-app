import React, {Component, ReactDOM} from "react";
import {Button} from 'antd'

export default class AntdTest extends Component {
    render() {
        return (
            <div>
                <Button type="primary">sadasdas</Button>
            </div>
        )
    }
}